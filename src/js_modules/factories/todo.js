export default class Todo {
  constructor(title, date, description, priority, completed) {
    this.title = title;
    this.date = date;
    this.description = description;
    this.priority = priority;
    this.completed = completed;
  }
}
